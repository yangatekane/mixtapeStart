<h2><%= @title %></h2>
<ul class="micropost">
	<%= form_for @micropost do |f| %>
		<div class="field">
			<%= f.text_area :content, :size => "60x12" %>
		</div>
		<div class="actions">
			<%= f.submit "Comment" %>
		</div>
	<% end %>
</ul>
